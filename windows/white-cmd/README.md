# Windows - Command Installation

Use PyPI if you prefer installing from the command line:

```powershell
pip install oktopios
okp-setup
okp --version
```

Then test:

```powershell
okp "print('Bonjour Oktopios')"
```